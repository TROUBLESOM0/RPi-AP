1. Check version of Debian
2. Install dependencies
3. Check if wireless radio exists
4. Check if web servers exists (which ones)
5. Install web server, if needed
6. Make backup of previous web server
7. Check for required files / Check version control
8. Option to download/upgrade version (this should be in RPi-Scripts)
9. 
