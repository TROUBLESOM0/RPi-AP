1. Check version of Debian
2. Check if wireless radio exists
3. Check if web servers exists (which ones)
4. Install web server, if needed
5. Make backup of previous web server
6. Check for required files / Check version control
7. Option to download/upgrade version (this should be in RPi-Scripts)
8. 
